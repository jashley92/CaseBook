@* Chromeless layout — no sidebar/nav, so pages using it show none of the case counts or navigation
   that the main shell surfaces. Used by the idle-lock page (F-15) so a locked screen reveals nothing. *@
@inherits LayoutComponentBase

<div class="blank-layout">
    @Body
</div>
