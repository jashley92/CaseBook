# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

# Rider / VS
.idea/
.vs/

# Claude Code local worktrees / scratch
.claude/worktrees/

# Local app data (dev database, evidence store, generated reports)
App_Data/
*.db
*.db-shm
*.db-wal
evidence-store/
report-output/
seals/

# Secrets / local config overrides
appsettings.*.local.json
secrets.json

# Internal planning docs — kept local, not published.
BACKLOG.md
BACKLOG-DONE.md
SECURITY-BACKLOG.md
UX-BACKLOG.md
PRODUCT-BACKLOG.md

# Filled-in install answers file (names AD groups/hosts/account) — the *.template.psd1 stays tracked.
casebook.config.psd1
# Generated production config (written on the target host by Install-CaseBook.ps1).
appsettings.Production.json
